#ifndef _SHELL_H_
#define _SHELL_H_
#define BUFFER
#define

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>
#include <sys/stat.h>

struct stat{
	
	
};

int main (void);
int main (int ac, char **av);
int main (int ac, char **av, **env);
char *strtok(char *str, const char *delim);
pid_t getppid(void);
ppid_t getppid(void);

#endif
